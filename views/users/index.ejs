<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="List of all users with links to their profiles.">
    <meta name="keywords" content="users list, user profiles, directory, view profile, contact information">
    <meta name="author" content="Eventix">
    <title>Users List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.0/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-900 flex flex-col items-center p-6">
    <header class="mb-8">
        <h1 class="text-4xl font-bold text-gray-800 text-center">Users List</h1>
    </header>
    <main class="w-full max-w-4xl bg-white p-8 rounded-lg shadow-lg">
        <table class="min-w-full divide-y divide-gray-200 table-fixed">
            <thead>
                <tr>
                    <th class="w-1/4 px-6 py-3 text-left text-xs font-medium text-blue-500 uppercase tracking-wider">Username</th>
                    <th class="w-1/4 px-6 py-3 text-left text-xs font-medium text-blue-500 uppercase tracking-wider">Email</th>
                    <th class="w-1/4 px-6 py-3 text-left text-xs font-medium text-blue-500 uppercase tracking-wider">Phone</th>
                    <th class="w-1/4 px-6 py-3 text-left text-xs font-medium text-blue-500 uppercase tracking-wider">Profile Link</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                <% users.forEach(user => { %>
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap text-gray-900">
                        <%= user.username %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-gray-900">
                        <%= user.email %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-gray-900">
                        <%= user.phone %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <a href="/users/<%= user.id %>" class="text-green-600 hover:underline" aria-label="View profile of <%= user.username %>">
                            View Profile
                        </a>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <div class="mt-6 flex justify-between">
            <a href="/users/create" class="bg-blue-500 text-white py-2 px-4 rounded-md shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Create a new user
            </a>
            <a href="/users/dash" class="bg-blue-500 text-white py-2 px-4 rounded-md shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Back to dashboard
            </a>
        </div>
    </main>
</body>
</html>
