<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="List of all users with links to their profiles.">
    <meta name="keywords" content="users list, user profiles, directory">
    <meta name="author" content="Your Name">
    <title>Users List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.0/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body class="bg-gray-100 text-gray-900 flex flex-col items-center p-6">
    <header class="mb-8">
        <h1 class="text-3xl font-bold">Users</h1>
    </header>
    <main class="w-full max-w-2xl bg-white p-6 rounded-lg shadow-lg">
        <table class="min-w-full divide-y divide-gray-200 flex-flex justify-between">
            <thead>
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Username</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Email</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Phone</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Profile Link</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                <% users.forEach(user => { %>
                <tr>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <%= user.username %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <%= user.email %>
                    </td><td class="px-6 py-4 whitespace-nowrap">
                        <%= user.phone %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                        <a href="/users/<%= user.id %>" class="text-blue-600 hover:underline">
                            View Profile
                        </a>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <a href="/users/create" class="mt-6 inline-block bg-blue-500 text-white py-2 px-4 rounded-md shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
            Create a new user
        </a>
        <div>
        <a href="/users/dash" class="mt-6 inline-block bg-blue-500 text-white py-2 px-4 rounded-md shadow-sm hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
            Back to dashboard
        </a>
        </div>
    </main>
</body>
</html>
